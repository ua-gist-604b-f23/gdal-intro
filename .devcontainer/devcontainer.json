{
	"name": "GDAL Intro",
	"image": "mcr.microsoft.com/vscode/devcontainers/base:buster",
  
	"customizations": {
	  "vscode": {
		"extensions": [		
			"analytic-signal.preview-tiff"			
	  ]
	  }
	},
	"postCreateCommand": "add-apt-repository -y ppa:ubuntugis/ubuntugis-unstable && apt-get update -y && apt-get install -y gdal-bin"
  }
